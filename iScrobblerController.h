////  iScrobblerController.h//  iScrobbler////  Created by Sam Ley on Feb 14, 2003.//  Released under the GPL, license details available at//  http://iscrobbler.sourceforge.net//#import <Cocoa/Cocoa.h>@class PreferenceController;@class KeyChain;@class AudioScrobblerProtocol;extern NSString *iScrobblerPrefsRecentSongsCountKey;extern NSString *iScrobblerPrefsClientVersionKey;extern NSString *iScrobblerPrefsClientIdKey;extern NSString *iScrobblerPrefsProtocolVersionKey;extern NSString *iScrobblerPrefsServerURLKey;@interface iScrobblerController : NSObject{    //the status item that will be added to the system status bar    NSStatusItem *_statusItem;    //the menu attached to the status item    IBOutlet NSMenu *theMenu;    //a timer which will let us check iTunes every 10 seconds    NSTimer *_iTunesUpdateTimer;    //the script to get information from iTunes    NSAppleScript *_iTunesStatusScript;    //stores info about the songs iTunes has played    NSMutableArray *_recentlyPlayedList;    //the preferences window controller    PreferenceController *_preferenceController;		BOOL _haveShownUpdateNowDialog;		AudioScrobblerProtocol *_submissionController;}- (IBAction)playSong:(id)sender;- (IBAction)clearRecentSongsMenu:(id)sender;- (IBAction)openPreferencesWindow:(id)sender;- (IBAction)openAudioScrobblerHomepage:(id)sender;- (IBAction)openiScrobblerDownloadPage:(id)sender;- (IBAction)openUserHomepage:(id)sender;- (void)showApplicationIsDamagedDialog;- (void)showBadCredentialsDialog;- (AudioScrobblerProtocol *)submissionController;- (PreferenceController *)preferenceController;- (NSString *)appSupportDirectory;void ISLog(NSString *function, NSString *format, ...);@end